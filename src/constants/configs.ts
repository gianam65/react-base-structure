export const APP_NAME = "React base structure";
export const APP_VERSION = "1.0.0";

export enum APP_LAYOUT {
  // Layout type depend on design
  BASE = "BASE",
  LOGIN = "LOGIN",
  NOT_FOUND = "NOT_FOUND",
}
